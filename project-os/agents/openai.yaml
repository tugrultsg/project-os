interface:
  display_name: "Project OS"
  short_description: "Bootstrap Claude/Codex workflows with private profiles"
  default_prompt: "Use $project-os to bootstrap this repository with my private profile and local secrets."
